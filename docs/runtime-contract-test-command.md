# Runtime contract test command

The runtime contract test can be run directly with:

```bash
node tests/runtime-app-contract.test.mjs
```

The package script update was intentionally left out of this first branch so the rearchitecture stays small and easy to review.
